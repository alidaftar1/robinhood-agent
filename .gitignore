.next/
node_modules/
.env*.local
*.tsbuildinfo
.vercel
.env*
.DS_Store
robinhood-autopilot-logs/

# This repo uses bun (bun.lock) — `npm install` (used locally to verify this
# fix, no bun available) generates its own lockfile that shouldn't be tracked
# alongside it.
package-lock.json
